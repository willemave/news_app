## 1. Findings (priority ordered)

1. Severity: **high** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/APIClient.swift:385-392` | Current behavior: `streamNDJSON` decodes each NDJSON line and on decode failure logs errors, then continues the stream. | Why it hides defects: malformed backend payloads are dropped silently, so the UI can miss partial assistant content with no terminal error. | Recommendation: **log+partial-fail** (emit structured stream-level failure and either surface partial-result warning or fail fast for strict mode).

2. Severity: **high** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/RealtimeTranscriptionService.swift:149-156` | Current behavior: final transcript wait timeout is caught and only logged; method still proceeds to return `currentTranscript`. | Why it hides defects: users get a successful return path even when commit/finalization failed, masking service-side transcription instability and producing potentially truncated output without explicit failure. | Recommendation: **error+fail**.

3. Severity: **medium** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/RealtimeTranscriptionService.swift:216-219` | Current behavior: invalid/missing `model` input is silently replaced with `defaultModel = "gpt-realtime"` before connect. | Why it hides defects: backend/model-provisioning contract bugs are masked, and sessions can connect with an unintended model while appearing healthy. | Recommendation: **error+fail** (reject invalid model and surface telemetry).

4. Severity: **medium** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/ViewModels/ChatSessionViewModel.swift:289-303` | Current behavior: if transcript refresh after polling fails, the fallback assistant message is appended locally. | Why it hides defects: message state can diverge from backend truth (missing final metadata, status, or processing errors), making backend API regressions hard to detect. | Recommendation: **log+partial-fail**.

5. Severity: **medium** | Location: `/Users/willem/Development/news_app/client/newsly/ShareExtension/ShareViewController.swift:187-210` | Current behavior: `loadItem` completion handlers ignore the error argument and only try best-effort URL extraction. | Why it hides defects: attachment-provider failures in Share sheet are silently dropped and may leave the extension with incomplete/incorrect URL extraction state. | Recommendation: **error+fail** (explicitly report extraction failures and block submission when no reliable URL is extracted).

6. Severity: **medium** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Views/ContentDetailView.swift:1196-1214` | Current behavior: discussion fetch errors open the fallback content URL directly without surfacing backend-fetch failure. | Why it hides defects: backend discussion API issues become a user-facing web-open path rather than a diagnosable in-app failure. | Recommendation: **log+partial-fail** (show clear in-app failure state and optional fallback action).

7. Severity: **low** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Views/Components/CachedAsyncImage.swift:124-142` and `/Users/willem/Development/news_app/client/newsly/newsly/Services/ImageCacheService.swift:161-168` | Current behavior: image decode/download failures fall back to placeholders; prefetch cache failures are intentionally silent. | Why it hides defects: broken image endpoints or serialization regressions are presented as UI placeholders without surfacing data quality issues. | Recommendation: **log+partial-fail** (track metrics for repeated image source failures per host/url).

8. Severity: **low** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/ChatGPTDeepLinkService.swift:15-29` and `/Users/willem/Development/news_app/client/newsly/newsly/Services/TwitterShareService.swift:38-55` | Current behavior: multi-step deep-link fallbacks (`app link -> known scheme -> web/app store/web intent`) with only success callback. | Why it hides defects: routing/provider failures degrade to web fallback and can hide broken deep-link or app-link registration/configuration issues. | Recommendation: **log+partial-fail** (always log chosen fallback reason).

9. Severity: **low** | Location: `/Users/willem/Development/news_app/client/newsly/newsly/Shared/SharedContainer.swift:91-97` | Current behavior: URL routing returns `.generic` on host parse/normalization failure. | Why it hides defects: malformed/unsupported URL inputs are silently treated as generic share paths rather than being surfaced for routing diagnostics. | Recommendation: **log+partial-fail**.

---

## 2. Keep-as-is cases (brief)

- Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/APIClient.swift:235-258` — Authentication-failure handling for 401/403 is explicit (`APIError.unauthorized` + auth-required notification) and should stay; it fails visibly instead of silently degrading.
- Location: `/Users/willem/Development/news_app/client/newsly/newsly/Services/OpenAIService.swift:26-39` — `refreshTranscriptionAvailability()` intentionally degrades capability state (`backendTranscriptionAvailable = false`) when probing fails; this is a clear capability toggle, not hidden data corruption.