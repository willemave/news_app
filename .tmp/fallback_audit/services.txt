## 1. Findings (priority ordered)

1) Severity: **high**  
Location: `[app/services/content_analyzer.py](/Users/willem/Development/news_app/app/services/content_analyzer.py):163-188`, `[app/services/content_analyzer.py](/Users/willem/Development/news_app/app/services/content_analyzer.py):339-352`  
Current behavior: `_fetch_page_content` returns `(None, None)` on any fetch/parsing exception, then `analyze_url` continues with empty HTML/text and a best-effort path.  
Why it hides defects: analysis can succeed with degraded input and produce an apparently valid `AnalysisError`/LLM output even though page acquisition failed, masking upstream ingestion defects.  
Recommendation: **error+fail** (or fail-fast unless an explicit fallback policy is configured for that endpoint).

2) Severity: **high**  
Location: `[app/services/content_metadata_merge.py](/Users/willem/Development/news_app/app/services/content_metadata_merge.py):66-70`, `[app/services/content_metadata_merge.py](/Users/willem/Development/news_app/app/services/content_metadata_merge.py):95-111`  
Current behavior: if latest metadata row is missing/invalid, it falls back to caller-provided `updated_metadata`.  
Why it hides defects: data-loss/inconsistency (missing DB row, bad query, stale content id) is masked as a normal merge, potentially applying incorrect state instead of surfacing the corruption path.  
Recommendation: **error+fail**.

3) Severity: **medium**  
Location: `[app/services/exa_client.py](/Users/willem/Development/news_app/app/services/exa_client.py):173-175`, `[app/services/exa_client.py](/Users/willem/Development/news_app/app/services/exa_client.py):281-283`, `[app/services/exa_client.py](/Users/willem/Development/news_app/app/services/exa_client.py):321-328`  
Current behavior: missing key or any client/search/error condition returns `[]` (empty list).  
Why it hides defects: external-search failures are indistinguishable from “no matching results,” so callers proceed with empty context and can silently produce weakly inferred outputs.  
Recommendation: **log+partial-fail** (for non-critical consumers) or **error+fail** when discovery/summarization explicitly depends on Exa results.

4) Severity: **medium**  
Location: `[app/services/feed_discovery.py](/Users/willem/Development/news_app/app/services/feed_discovery.py):984-987`, `[app/services/feed_discovery.py](/Users/willem/Development/news_app/app/services/feed_discovery.py):1058-1066`, `[app/services/feed_discovery.py](/Users/willem/Development/news_app/app/services/feed_discovery.py):1110-1120`  
Current behavior: URL normalization/feed lookup failures are swallowed and candidate is dropped (`None`) without bubbling as run-level failure.  
Why it hides defects: transient parser/API/normalization failures reduce candidate discovery silently and lower confidence in discovery coverage.  
Recommendation: **log+partial-fail** with explicit failure metrics; propagate hard if all candidates for a lane become invalid.

5) Severity: **medium**  
Location: `[app/services/feed_detection.py](/Users/willem/Development/news_app/app/services/feed_detection.py):337-351`, `[app/services/feed_detection.py](/Users/willem/Development/news_app/app/services/feed_detection.py):388-410`, `[app/services/feed_detection.py](/Users/willem/Development/news_app/app/services/feed_detection.py):523-540`  
Current behavior: `classify_feed_type_with_llm` returns `None` on model/config/parse errors, and `classify_feed_type` falls back to heuristic result.  
Why it hides defects: invalid classification failures become low-confidence heuristic defaults, which can drive wrong downstream feed-type persistence decisions.  
Recommendation: **error+fail** when LLM is explicitly enabled and no reliable heuristic threshold is met.

6) Severity: **medium**  
Location: `[app/services/news_processing.py](/Users/willem/Development/news_app/app/services/news_processing.py):217-227`, `[app/services/news_processing.py) (/Users/willem/Development/news_app/app/services/news_processing.py):231-250`  
Current behavior: non-`NewsSummary` summarization output is replaced by `_fallback_summary`, then persisted as success.  
Why it hides defects: partial/invalid model output is converted into synthetic digest content, masking model/data issues and potentially contaminating processed records.  
Recommendation: **log+partial-fail** (mark as degraded and keep trace of fallback), not silent success.

7) Severity: **low**  
Location: `[app/services/news_digests.py](/Users/willem/Development/news_app/app/services/news_digests.py):469-496`, `[app/services/news_digests.py](/Users/willem/Development/news_app/app/services/news_digests.py):516-531`  
Current behavior: any digest-agent exception falls back to heuristic bullet/header drafts.  
Why it hides defects: digest generation failures are masked as completed content, making downstream quality regressions harder to detect.  
Recommendation: **log+partial-fail** (surface fallback usage and allow monitoring to alert).

## 2. Keep-as-is cases (brief)

- `[app/presenters/content_presenter.py](/Users/willem/Development/news_app/app/presenters/content_presenter.py):195-205` is intended as a presentation-level resilience path (`build_fallback_content_summary_response`) to keep list/detail APIs responsive when optional display data is incomplete.  
- `[app/services/langfuse_tracing.py](/Users/willem/Development/news_app/app/services/langfuse_tracing.py):87-154`, `[app/services/langfuse_tracing.py](/Users/willem/Development/news_app/app/services/langfuse_tracing.py):156-167` should remain: observability is optional by design and should not block core content workflows.