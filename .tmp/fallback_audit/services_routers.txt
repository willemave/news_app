## 1. Findings (priority ordered)

1. `Severity: high`
   - `Location: /Users/willem/Development/news_app/app/services/llm_summarization.py:445-466`  
   - `Current behavior`: Summarization attempts catch broad exceptions across retry/fallback paths and return `None` instead of failing the operation.  
   - `Why it hides defects`: Upstream model/provider failures are converted into an apparently successful pipeline step with a missing summary payload.  
   - `Recommendation`: `error+fail` (return/mark explicit failure state instead of `None` when all retries are exhausted).

2. `Severity: medium`
   - `Location: /Users/willem/Development/news_app/app/services/feed_detection.py:340-410`  
   - `Current behavior`: LLM-based feed classification swallows provider/config errors and returns `None`, then caller-level heuristics run as a fallback.  
   - `Why it hides defects`: Actual inference failures become indistinguishable from legitimate “no clear signal” cases, which can silently force weak, heuristic feed typing.  
   - `Recommendation`: `log+partial-fail` (always emit a structured classification-failure event with reason and confidence metadata).

3. `Severity: medium`
   - `Location: /Users/willem/Development/news_app/app/services/feed_detection.py:555-565, 676-696`  
   - `Current behavior`: Feed URL validation failures and discovery errors are swallowed and candidates are dropped, with eventual empty feed list return.  
   - `Why it hides defects`: Failures across all discovery strategies are surfaced as “no feeds found,” not “detection failed,” masking network/parsing regressions.  
   - `Recommendation`: `error+fail` when all discovery attempts fail; keep empty results only when a specific attempt fails non-critically.

4. `Severity: medium`
   - `Location: /Users/willem/Development/news_app/app/services/content_analyzer.py:186-188, 339-352`  
   - `Current behavior`: Fetch/parsing failures return `(None, None)` and processing continues into fallback analysis path.  
   - `Why it hides defects`: Invalid upstream fetch state is masked as a normal low-confidence content analysis and can still persist with wrong type/metadata.  
   - `Recommendation`: `log+partial-fail` (record explicit fetch-failure reason and require explicit “partial data” marker on output).

5. `Severity: medium`
   - `Location: /Users/willem/Development/news_app/app/services/image_generation.py:396-407, 518-534, 610-626`  
   - `Current behavior`: Broad exceptions are converted into `None` / `failed` result objects instead of raising.  
   - `Why it hides defects`: Downstream code can ignore/under-handle the degraded status and continue as if image generation was non-critically unavailable.  
   - `Recommendation`: `log+partial-fail` and ensure consumers always branch on status + reason before continuing.

6. `Severity: medium`
   - `Location: /Users/willem/Development/news_app/app/services/onboarding.py:250-266, 292-306, 1051-1060, 1230-1251`  
   - `Current behavior`: Broad exceptions return synthetic onboarding/audio-plan defaults (`used_fallback` variants).  
   - `Why it hides defects`: Model/parser failures become “valid” defaults and can silently lower quality without obvious operator signal.  
   - `Recommendation`: `log+partial-fail` (keep fallback, but emit persistent metrics/events for fallback activation and reason).

7. `Severity: low`
   - `Location: /Users/willem/Development/news_app/app/routers/api/submission.py:145-157`  
   - `Current behavior`: Per-item serialization errors are caught and the bad record is skipped from status list responses.  
   - `Why it hides defects`: Clients receive partial data with no per-item failure surface; data issues look like transient missing rows.  
   - `Recommendation`: `log+partial-fail` with explicit omission reason in response metadata.

8. `Severity: low`
   - `Location: /Users/willem/Development/news_app/app/routers/api/chat.py:243-244, 373-375`  
   - `Current behavior`: Malformed messages are dropped during extraction/preview rendering.  
   - `Why it hides defects`: Chat history appears intact but with silent content loss.  
   - `Recommendation`: `log+partial-fail` and include a safe placeholder entry or warning count.

9. `Severity: low`
   - `Location: /Users/willem/Development/news_app/app/routers/api/narration.py:55-59`  
   - `Current behavior`: Narration presenter metadata failures are swallowed, fallbacking to derived title.  
   - `Why it hides defects`: Bad metadata pipeline states are hidden while narration generation still proceeds.  
   - `Recommendation`: `log+partial-fail` (surface a non-blocking fallback reason in narration payload).

## 2. Keep-as-is cases (brief)

- `app/services/feed_detection.py` multi-method discovery chain (HTML feed tags → anchors → candidate probing → Exa) is a reasonable fallback strategy for availability resilience; just ensure final “no feeds found” includes a structured failure summary when all methods fail.
- `app/services/image_generation.py` returning explicit failed `ImageGenerationResult` is better than uncaught crashes, provided callers reliably branch on status.
- `app/services/content_analyzer.py` and similar parse-only helpers that degrade only to missing media/audio metadata are acceptable as non-critical best-effort enrichments.
- `app/services/onboarding.py` explicit `used_fallback` outputs are product-friendly, as long as the flag is exposed and monitored.